name: Shell Commands
on: [push]

jobs:
  run-shell-commend:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "a message from Github Actions"
      - name: multiline script
        run: |
          node -v
          npm -v
